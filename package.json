{
  "name": "tree-sitter-chialisp",
  "version": "1.0.7",
  "description": "A tree-sitter parser for Chialisp, the language used to build smart coins on top of the Chia blockchain.",
  "main": "bindings/node",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "quexington",
  "license": "ISC",
  "dependencies": {
    "nan": "^2.14.2"
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.19.5"
  }
}
